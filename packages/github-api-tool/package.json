{
  "name": "github-api-tool",
  "version": "0.1.0",
  "description": "GitHub API CLI tool for AI agents",
  "private": true,
  "//": "Internal/local CLI — intentionally NOT published to npm (no consumers; this repo publishes only orchestrator-workflow). The HIGH token-config owner-only-perms fix (PR #42) ships in source and runs from source. Posture recorded for agent-tasks 36f8439e.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "github": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "vitest run",
    "test:ci": "vitest run --coverage"
  },
  "keywords": [
    "github",
    "api",
    "cli"
  ],
  "author": "Lan Nguyen Si",
  "license": "MIT",
  "dependencies": {
    "@octokit/rest": "^20.0.0",
    "chalk": "^5.0.0",
    "commander": "^11.0.0",
    "date-fns": "^4.1.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^4.1.9",
    "typescript": "^5.0.0",
    "vitest": "^4.1.6"
  },
  "overrides": {
    "postcss": "^8.5.10"
  }
}
