import { describe, expect, it } from "vitest";

describe("alpha", () => {
  it("does a", () => {
    expect(1).toBe(1);
  });
});

describe("beta", () => {
  it("does b", () => {
    expect(2).toBe(2);
  });
});
