---
type: reference
title: Fixture doc for prose-line-references binding-rule discrimination
---

# Sample

`src/fileA.ts` sets the const. `src/fileB.ts` defines a helper. This function documented in `src/fileC.ts` reads it on line 1.

See line 2 in `src/fileD.ts` for the details.

`src/fileE.ts` implements the parser. See line 1 for the details.

Configured in `src/eg.ts`, e.g. line 1 sets the default.

Configured in `src/ie.ts`, i.e. line 1 sets the default.

Compare `src/vsold.ts` vs. line 1 in the new version.

See the parser in `src/parser.ts`. Now check line 1 there.
